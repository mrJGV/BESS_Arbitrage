# --- Private working notes ------------------------------------------------
# Published rationale lives in docs/DECISIONS.md.
docs/private/

# --- Secrets --------------------------------------------------------------
.env
*.token
esios_token*

# --- Throwaway spikes -----------------------------------------------------
scratch/

# --- Python ---------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# --- Environments ---------------------------------------------------------
.venv/
venv/
env/

# --- Tool caches ----------------------------------------------------------
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/

# --- Generated output -----------------------------------------------------
# Backtest artefacts are reproducible; the frozen data snapshot and the
# committed reference results under results/ are deliberately NOT ignored.
figures/
outputs/
*.log
hpc/logs/

# results/ is committed: the annual bounds, the backtest summaries the README
# quotes (with per-day profit, so its intervals can be recomputed) and the
# headline chart drawn from them. `make backtest` and `make figures`
# regenerate the last two.

# --- OS / editor ----------------------------------------------------------
.DS_Store
Thumbs.db
.vscode/
.idea/
